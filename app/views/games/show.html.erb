<h2>Game #<%=@game.id%> on <%=@game.map.name%></h2>


<%= render :partial => "scoreboard" %>

  <table class="map map-game" data-add-unit-path="<%= add_unit_game_path(@game) %>" data-move-unit-path="<%= move_unit_game_path(@game) %>">
  	<%
  		x = 0
  		y = 0
  	%>
  	<tr>
  	<% for tile in @game.map.tiles %>
  		<% if x >= @game.map.width 
  			x = 0
  			y += 1 %>
  			</tr><tr>
  		<% end %>
  		<td class="tile" data-x="<%=x%>" data-y="<%=y%>" data-tile_type_tag="<%=tile.tile_type.tag %>">
        <% game_unit = @game.get_game_unit_at(x,y) %>
        <% unless game_unit.nil? %>
          <div class="unit" data-unit_id="<%=game_unit.id%>" data-unit_tag="<%=game_unit.unit.tag%>" data-team_id="<%=game_unit.team_id%>"></div>
        <% end %>
      </td>
  		<% x += 1 %>
  	<% end %>
  		</tr>
  </table>

  <% if @player.team_id == @game.current_team_id %>
  <div class="tool-button" data-tool_tag="pointer"></div>
  <% for unit in Unit.all %>
    <div class="unit-button" data-unit_tag="<%=unit.tag %>"></div>
  <% end %>
  <br class="clear">
  <div class="actions">
    <%= button_to "End Turn", end_turn_game_path(@game) %>
  </div>
  <% end %>




<%= link_to 'Back to Game List', games_path %>
