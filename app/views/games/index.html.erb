<h1>Listing games</h1>

<% unless params[:id].blank? %>
  <%= link_to "Show Everyone's Games!", games_path, :class => "btn" %>
<% end %>

<table class="game-list table table-striped">
  <tr>
    <th>Players</th>
    <th>Map</th>
    <th>Status</th>
    <th></th>
  </tr>

<% @games.each do |game| %>
<% begin %>
  <tr>
    <td><%= game.players.map {|p| n(p.user)}.join(", ") %></td>
    <td><%= game.map.name %></td>
    <td class="<%= !game.current_player.nil? and game.current_player.user==current_user ? "your-turn" : ""%>">
      <%= game.winning_player_id.nil? ? "#{n(game.current_player.user)}'s Turn" : "Complete" %>
    </td>
    <td><%= link_to 'Play!', game, :class => "btn" %></td>
  </tr>
<% rescue %>
    <% raise game.inspect %>
<%end %>
<% end %>
</table>

<br />

<%= link_to 'New Game', new_game_path %>
